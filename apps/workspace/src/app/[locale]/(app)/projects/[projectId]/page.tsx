/**
 * Registers the canonical Project detail route.
 *
 * The parent layout owns the authorized Project query and complete detail UI;
 * this leaf exists so Next.js resolves `/projects/[projectId]` to that layout.
 */
export default function ProjectDetailPage() {
  return null;
}
